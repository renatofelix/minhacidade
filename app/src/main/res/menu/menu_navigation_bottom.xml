<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/novos"
        android:title="Novos"
        android:icon="@drawable/ic_hot" />

    <item android:id="@+id/enviados"
        android:title="Logar"
        android:icon="@drawable/ic_meus" />
</menu>